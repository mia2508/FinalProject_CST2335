<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vipha.finalproject_cst2335">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MovieActivity" />
        <activity android:name=".CBCActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".BusActivity" />
        <activity android:name=".food_favorite" />
        <activity android:name=".FoodFavorities" />
        <activity android:name=".FoodToolbar" />
        <activity android:name=".FoodInformation" />
        <activity android:name=".FinalDisplay"></activity>
    </application>

</manifest>